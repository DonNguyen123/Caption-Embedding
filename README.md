# Caption-Embedding
This code essentially allows you to embed vtt caption files into a mp4 file, meaning you get toggleable captions.  Note that this code should be run on a local 8000 server, and not directly by clicking the html. This allows the libraries to load correctly.
